export type ServiceProvider = {
  owner: string
  endpoint: string
  spID: number
  type: string
  blockNumber: number
  delegateOwnerWallet: string
}

export type PeerInfo = {
  ip: string
  nkey: string
}
