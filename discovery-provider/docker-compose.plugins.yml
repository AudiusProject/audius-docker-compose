version: "3.9"

services:
  notifications:
    image: audius/discovery-provider-notifications:${TAG:-1660bbc3f251e0feeb2bb2ed1585c7dfaae6d533}
    container_name: notifications
    restart: unless-stopped
    networks:
      - dn-network
    env_file:
      - ${NETWORK:-prod}.env
      - ${OVERRIDE_PATH:-override.env}
    logging:
      options:
        max-size: 10m
        max-file: 3
        mode: non-blocking
        max-buffer-size: 100m
      driver: json-file
    ports:
      - "6000:6000"

  sla-auditor:
    image: audius/sla-auditor:${TAG:-1660bbc3f251e0feeb2bb2ed1585c7dfaae6d533}
    container_name: sla-auditor
    restart: unless-stopped
    networks:
      - dn-network
    env_file:
      - ${NETWORK:-prod}.env
      - ${OVERRIDE_PATH:-override.env}
    logging:
      options:
        max-size: 10m
        max-file: 3
        mode: non-blocking
        max-buffer-size: 100m
      driver: json-file

  dashboard:
    image: audius/dashboard:${TAG:-1660bbc3f251e0feeb2bb2ed1585c7dfaae6d533}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - dn-network

networks:
  dn-network:
    name: discovery-provider_discovery-provider-network
    external: true
