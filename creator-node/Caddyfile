{
    order cache before rewrite
    order request_header before cache
    debug
    servers :443 {
        protocols h1 h2
    }
}

{$creatorNodeEndpoint} {
  {$CADDY_TLS}
  encode zstd gzip

  reverse_proxy audiusd:80
}
