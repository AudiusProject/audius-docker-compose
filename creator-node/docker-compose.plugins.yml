version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-3a176a2041c8a24ca341cef0b5713237fb3effde}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
