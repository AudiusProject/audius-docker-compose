version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-c6fed0ad0260090463a73bcd457a87317d83ccfa}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
