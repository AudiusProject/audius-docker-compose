version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-fc9f4f8d0bece563a61747d295a81a4028117e40}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
