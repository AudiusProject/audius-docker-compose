version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-51de8660844428091c8a85b50c3c0fa4eec95cb8}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
