version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-e176fece62be591c420e9f789bf1e8db0669bf45}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
