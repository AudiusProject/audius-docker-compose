version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-d6b5fee2dca6bad9b0d30bfb2cec8eea48aee2a8}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
