version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-3482fe3189fc9953b78adf5604eabd015704a1ef}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
