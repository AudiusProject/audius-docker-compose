version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-da10685ccd206135f25563efecc976bdbeef2a1a}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
