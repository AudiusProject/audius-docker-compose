version: "3.9"

services:
  dashboard:
    image: audius/dashboard:${TAG:-50882b925d4dcc9abebf6f12c9078c4b632e0e8f}
    container_name: dashboard
    restart: unless-stopped
    command: ["npm", "run", "start:${NETWORK:-prod}"]
    networks:
      - cn-network

networks:
  cn-network:
    name: creator-node_creator-node-network
    external: true
